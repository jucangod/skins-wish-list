import React from "react";

function SkinsWishButton({findSection}) {
    return(
        <button
            className="wishButton"
            // onClick={findSection}
        >
            Wish
        </button>
    );
};

export { SkinsWishButton }